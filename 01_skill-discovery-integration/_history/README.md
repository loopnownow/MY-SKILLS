# History (non-live)

Files here are historical snapshots, kept for audit trail only. They are **not** referenced
by the live routing/mount logic. Canonical current file is `../registry.yaml`.

- `registry.v3.30.yaml` — last v3 registry (30 coarse ids) before the v4 (CHG-20260913-001) rewrite to 10 coarse + 52 fine ids.
- `registry_v4.audit.yaml` — the research/audit working file used to derive v4's `registry.yaml` (source-by-source scan of MedSci/Scientific/OpenClaw/AIPOCH/Nature).
